/**
 * Sandbox core interface definitions
 */

// ==================== Sandbox configuration ====================
export interface SandboxConfig {
  timeframe: string; // Candlestick interval, e.g. '1h', '1d', '15m'
}
